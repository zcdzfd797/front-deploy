[package]
name = "front-deploy-tauri"
version = "1.0.0"
description = "Front Deploy desktop app powered by Tauri"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
